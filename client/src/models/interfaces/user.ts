export interface IUserDTO {
    id: string;
    first_name: string;
    last_name: string;
}

export interface IUser {
    id: string;
    firstName: string,
    lastName: string,
    username: string,
    password: string,
    email: string
}
